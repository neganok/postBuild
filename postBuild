#!/bin/bash

echo "--------------------------------------------------"
echo "🔄 Sao chép file từ repo..."
echo "--------------------------------------------------"

# Sao chép toàn bộ nội dung từ thư mục hiện tại vào thư mục home (~)
cp -r . ~

# Kiểm tra nếu file đã được sao chép
echo "✅ Kiểm tra file đã copy:"
ls -lah ~

echo "--------------------------------------------------"
echo "🔄 Cài đặt npm package..."
echo "--------------------------------------------------"

# Cài đặt các package cần thiết
npm install hpack https commander colors socks

echo "🚨 Hủy deploy..."
exit 1
